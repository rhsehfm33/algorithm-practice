#include <bits/stdc++.h>

using namespace std;

typedef long long ll;

void solve() {
    int N;
    cin >> N;
    while (N >= 0) {
        cout << N-- << '\n';
    }
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    
    solve();

    return 0;
}