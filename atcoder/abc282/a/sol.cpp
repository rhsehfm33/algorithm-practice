#include <bits/stdc++.h>

using namespace std;


void solve() {
    int K;
    cin >> K;
    for (int i = 0; i < K; ++i) {
        cout << char('A' + i);
    }
    cout << '\n';
}

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    
    solve();

    return 0;
}